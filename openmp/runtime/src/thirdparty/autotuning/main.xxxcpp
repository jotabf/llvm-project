#include "Autotuning.h"
#include <iostream>

int main() {

  double min[1] = {0};
  double max[1] = {100};
  Autotuning *autotuning = new Autotuning(min, max, 1, new CSA(1, 1, 1));

  delete autotuning;
  return 0;
}